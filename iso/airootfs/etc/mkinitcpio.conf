# Arches mkinitcpio.conf — hardware-agnostic (no autodetect)
# The kms hook loads GPU drivers for all supported hardware.

MODULES=()
BINARIES=()
FILES=()
HOOKS=(base udev modconf kms keyboard keymap block filesystems fsck)
