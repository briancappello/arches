#
# Arches pacman.conf — aarch64-apple with Asahi Linux repos
# Asahi repos above ALARM repos for Apple Silicon-specific packages
#

[options]
HoldPkg     = pacman glibc
Architecture = aarch64
SigLevel    = Required DatabaseOptional
LocalFileSigLevel = Optional

# Asahi Linux repos (Apple Silicon kernel, mesa, firmware)
[asahi]
Server = https://cdn.asahilinux.org/arch/$repo/$arch

# Arch Linux ARM repos
[core]
Server = http://mirror.archlinuxarm.org/$arch/$repo

[extra]
Server = http://mirror.archlinuxarm.org/$arch/$repo

[alarm]
Server = http://mirror.archlinuxarm.org/$arch/$repo

[aur]
Server = http://mirror.archlinuxarm.org/$arch/$repo
