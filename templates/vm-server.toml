[meta]
name = "VM Server"
description = "Headless server — nginx, postgres, redis, SSH"

[system]
timezone = "America/Denver"
locale = "en_US.UTF-8"

[modules]
include = [
    # Base
    "base",
    "zsh",
    # Networking
    "networking",
    # Services
    "postgresql",
    "redis",
    "nginx",
]
