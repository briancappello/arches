# Enable NVIDIA Dynamic Power Management (RTD3)
#
# 0x02 = fine-grained power management — the GPU can enter D3
# when idle and wake on demand. Requires the udev rules in
# /etc/udev/rules.d/80-nvidia-rtd3.rules to set power/control=auto.
options nvidia NVreg_DynamicPowerManagement=0x02
